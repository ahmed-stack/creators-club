import styled from "styled-components";

// Local Imports
import { bodyBgColor } from "styles/globals";

export const LayoutWrapper = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: ${bodyBgColor};
`;
